export const screenPadding = 16;
export const cardGap = 8;
export const sectionGap = 20;

export const radius = {
  badge: 8,
  row: 10,
  card: 12,
  pill: 999,

  // legacy aliases — prefer named tokens above for new code
  sm: 10,
  md: 12,
  lg: 12,
  xl: 12,
} as const;
